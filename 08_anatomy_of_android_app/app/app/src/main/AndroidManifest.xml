<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SocialRoboticsLab"
        android:usesCleartextTraffic="true"
        tools:targetApi="36">

        <!-- 首頁 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 統一設定頁 -->
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".UnitEntryActivity" android:exported="false" />

        <!-- 對話設計（Pipecat Pipeline 設定） -->
        <activity android:name=".design.ConversationDesignActivity" android:exported="false" />

        <!-- Ch2: HTTP Chat（全螢幕表情 + Robot TTS/STT） -->
        <activity
            android:name=".http.HttpChatActivity"
            android:exported="false"
            android:theme="@style/Theme.SocialRoboticsLab.Fullscreen" />

        <!-- Ch3: WebSocket Audio（全螢幕表情 + 音訊串流） -->
        <activity
            android:name=".websocket.AudioStreamActivity"
            android:exported="false"
            android:theme="@style/Theme.SocialRoboticsLab.Fullscreen" />

        <!-- Ch4: WebRTC Video Streaming -->
        <activity android:name=".streaming.VideoStreamActivity" android:exported="false" />

        <!-- Ch6: WoZ Controller（全螢幕表情 + 指令接收） -->
        <activity
            android:name=".woz.WozRobotActivity"
            android:exported="false"
            android:theme="@style/Theme.SocialRoboticsLab.Fullscreen" />

        <!-- Ch5: WebRTC Video Call -->
        <activity android:name=".videocall.VideoCallLobbyActivity" android:exported="false" />
        <activity android:name=".videocall.VideoCallActivity" android:exported="false" />

    </application>
</manifest>
